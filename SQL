INSERT INTO table_name (column1, column2, ... )
VALUES (value1-1, value1-2, ... ), 
(value2-1,value2-2,....) # insert one or multiple value at one time
;

UPDATE table_name
SET column1 = value1, column2 = value2, ...
WHERE condition
;

DELETE FROM table_name
WHERE condition
;

select * from employees where address = 'Elgin,IL' ;

select * from employees where department = '5' and (Salary 

selecet first_name, lastname, d_numer from employees group by departname_number desc and order by lastname

selec

SELECT F_NAME, L_NAME, DEP_NANE
FROM EMPLOYEES,DEPARTMENTS group by DEP_NAME desc, L_
ORDER BY DEP_ID DESC, L_NAME DESC;

SELECT DEP_ID, COUNT(*) as "NUM_EMPLOYEES", AVG(SALARY) as "AVG_SALARY"
FROM EMPLOYEES 
GROUP BY DEP_ID having count(*) < 4 ORDER BY AVG(SALARY);


####Functions 
1. selecet Sum(Cost) from PETRESCUE-
2. selecet Sum(Cost) as SUM_OF_COST From PETRESCUE 
3. select max(quantity) from PETRESCUE;
4. select avg(Cost) from PETRESCUE-;
5. select avg(cost/quantity) from PETRESCUE where animal = "Dog";

B1. select ROUND(Cost) from PETRESCUE;
b2. select length(name) from PETRESCUE;
b3. select Ucase(name) from PETRESCUE;
b4. select Ucase(distinct(name)) from PETRESCUE; --> select DISTINCT(UCASE(ANIMAL)) from PETRESCUE;
Enter a query that displays all the columns from the PETRESCUE table, where the animal(s) rescued are cats. Use cat in lower case in the query.
b5. select * from PETRESCUE where animal = "Cat" as "cat"  --> select * from PETRESCUE where LCASE(ANIMAL) = 'cat'

C1. select Day(rescuredate) from PETRESCURE where animal ="Cat";
C2. select Sum(quantity) from PETRESCURE where month(rescuredate) = '05';
C3. select sum(quantity) from PETRESCURE where day(rescuredate) = '14';
C4. selcet (rescuredate + 3) from PETRESCURE;
C5. select (current date - rescuredate) from PETRESCURE;

# Sub-Queries
select * from Employees where salary < (select avg(salary) from Employees);
select emp_id, salary, (select Max(Salary) from employees) as MAX_SALARY from employees
select * from EMPLOYEES where JOB_ID In (Select JOB_IDENT from JOBS); 
  // The IN operator allows you to specify multiple values in a WHERE clause.
  
Inner Join 可以用2种方法写：
1. SELECT column_name(s)
    FROM table1 INNER JOIN table2
    ON table1.column_name = table2.column_name;
2. select column_name(s) from table1, table2 where table1.column_name = table2.column_name;
